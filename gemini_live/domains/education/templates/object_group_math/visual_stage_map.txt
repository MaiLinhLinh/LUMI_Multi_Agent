VISUAL STAGE MAP — MÀN HÌNH BÀI TOÁN

┌──────────────────────┐   {{ data.operator }}   ┌──────────────────────┐   =   ┌──────────────────────┐
│ Nhóm bên trái        │                │ Nhóm bên phải        │       │ Kết quả              │
│ {{ data.left_count }} {{ data.asset_label }}{% for _ in range(16 - (data.left_count|string|length) - (data.asset_label|length)) %} {% endfor %}│                │ {{ data.right_count }} {{ data.asset_label }}{% for _ in range(15 - (data.right_count|string|length) - (data.asset_label|length)) %} {% endfor %}│       │ Vật thể: {{ stage.result_items_text }}{% for _ in range(10 - (stage.result_items_text|length)) %} {% endfor %}│
│                      │                │                      │       │ Trạng thái: {{ stage.result_items_state }}│
│                      │                │                      │       │ Số: {{ stage.result_text }}{% for _ in range(18 - (stage.result_text|length)) %} {% endfor %}│
│                      │                │                      │       │ Trạng thái: {{ stage.answer_state }}│
└──────────────────────┘                └──────────────────────┘       └──────────────────────┘
[anchor: a]                             [anchor: b]                    [anchor: c = vật thể kết quả]
                                                                            [anchor: e = số kết quả]

Biểu thức phía dưới: {{ data.left_count }} {{ data.operator }} {{ data.right_count }} = ?
[anchor: d]

ANCHOR LEGEND
a = nhóm vật thể bên trái
b = nhóm vật thể bên phải
c = các vật thể kết quả
d = biểu thức
e = số kết quả
