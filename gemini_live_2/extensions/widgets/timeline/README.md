# timeline

Widget minh hoạ 2–6 mốc theo thứ tự, dùng cho sự kiện lịch sử, vòng đời hoặc quy trình.
`items` gồm các object `{label, title, description}`. Mỗi mốc có anchor riêng `milestone_1…n`,
nên Gemini Live có thể dùng `highlight`, `circle`, `pulse` hoặc `spotlight` vào đúng mốc đang kể.
